# auto-minimizer
This Python script minimizes all open windows on your Windows system when it detects that you are not looking at the screen. It uses a model trained using Liner ( for simplicity). The model is not trained on a large image dataset ( but I will try doing so later on)
